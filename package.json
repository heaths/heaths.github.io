{
  "private": true,
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.10.0",
    "prettier": "^3.8.1",
    "sequoia-cli": "^0.5.1"
  },
  "scripts": {
    "format": "prettier --write .",
    "prettier": "prettier",
    "sequoia": "sequoia"
  }
}
