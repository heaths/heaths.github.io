---
name: format
description: >
  Format changed files with Prettier. Run after all other changes in a prompt
  are complete.
---

After all changes in response to a prompt are complete, format modified files.

## Installation

Run `npm i` from the repository root to install dependencies. Run tools using
`npm run`.

## Steps

1. Run `npm run format` to format the full repository.
